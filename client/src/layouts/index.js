import React from 'react'

const AuthLayouts = ({children}) => {
  return (
    <>
        <header>
            Logo
        </header>
        {
        children
        }
    </>
  )
}

export default AuthLayouts
